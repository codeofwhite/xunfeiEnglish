#配置端口号
server:
  port: 8001

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/eng_teach?serverTimezone=GMT%2B8
    username: root
    password: Jason20040903
  servlet:
    multipart:
      enabled: true
      location: uploads
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: engteach.entity